import baquero from "./definitions-baquero.mjs";
import independent from "./definitions-independent.mjs";
import vargas from "./definitions-vargas.mjs";

const records = [...vargas, ...independent, ...baquero];

export default records;

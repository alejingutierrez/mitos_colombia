export const metadata = {
  title: {
    absolute: "Administracion | Mitos de Colombia",
  },
  robots: {
    index: false,
    follow: false,
    nocache: true,
  },
};

export default function AdminRouteLayout({ children }) {
  return children;
}

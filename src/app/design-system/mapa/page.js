import { MapTemplate } from "../../../components/templates";

export const metadata = {
  title: "Sistema de diseño · Mapa",
  robots: { index: false, follow: false },
};

export default function MapPreviewPage() {
  return <MapTemplate />;
}

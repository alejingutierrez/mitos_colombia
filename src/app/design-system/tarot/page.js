import { TarotTemplate } from "../../../components/templates";

export const metadata = {
  title: "Sistema de diseño · Tarot",
  robots: { index: false, follow: false },
};

export default function TarotPreviewPage() {
  return <TarotTemplate />;
}
